ARG DOCKER_BASE_IMAGE
FROM ${DOCKER_BASE_IMAGE}

ARG DOCKER_OS_LANG
ARG DOCKER_OS_LC_ALL
ARG APP_NAME
ARG APP_VERSION
ARG APP_BINARY

ENV LANG ${DOCKER_OS_LANG}
ENV LC_ALL ${DOCKER_OS_LC_ALL}

RUN pacman --noconfirm -Sy salt

RUN mkdir -p /opt/${APP_NAME}

WORKDIR /opt/${APP_NAME}
